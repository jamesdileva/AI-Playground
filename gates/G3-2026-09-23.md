# Gate G3 record - 2026-09-23 (Sprint 3)

Operator: opencode, session with user j. Node v24.14.1, Windows, localhost only.

Result: PASS - all thirteen criteria verified locally (13 automated tests +
three manual load runs) and hosted CI green on both runners (run
35942925517, ubuntu-latest + windows-latest).

## Standing criteria

- Pipeline `npm ci` -> `typecheck` -> `lint` -> `test` -> `build`: all green.
  Vitest 5 files / 40 tests (27 carried, 13 new S3). `npm audit`: 0
  vulnerabilities.
- No new runtime dependencies (only Hono subpath imports already installed).
- Manual runs below used the compiled server (`dist/server.js`) on
  127.0.0.1:3000 unless noted; temp harness scripts lived outside the repo
  (or as deleted `.tmp.mjs` files) and are not committed.

## Criteria

- 3.1 PASS. A `wait=25` GET blocked on an empty room returns 200 with the new
  message + fresh cursor within ~1 s of the POST (wake path, automated).
- 3.2 PASS. `wait=1` on an empty room returns 200 `[]` with unchanged cursor
  after ~1 s (automated).
- 3.3 PASS. `wait=25` clamps internally and a message posted mid-wait wakes
  the poller with the message (automated, 30 s timeout for Windows CI).
- 3.4 PASS. `wait=-1` and `wait=abc` -> `400 bad_wait` naming the valid
  range; `wait=9999` clamps to 25 s (automated).
- 3.5 PASS (manual, split-process). Server in its own process (file-logged
  waiter count + RSS every 500 ms); client opened 500 `wait=25` polls across
  all five rooms. All 500 registered; after client-side abort the registry
  drained to 0 within one 500 ms sample, and server RSS after settle was
  42,496,000 bytes vs a 42,496,000 baseline (ratio 1.0, within ±10%).
  Disconnect handling: the abort listener resolves the waiter, clears the
  timer, and deletes the room entry when its set empties.
- 3.6 PASS. Second POST within 8 s -> `429 cooldown` with `retry_after: 8`;
  posting again after 8 s succeeds (automated, injected clock).
- 3.7 PASS. Cooldown is per room: blocked in kitchen, free in balcony at the
  same timestamp (automated).
- 3.8 PASS. The 61st message in an hour -> `429 hourly_cap` (automated,
  injected clock, alternating agents). Also observed live in 3.10/3.13 below.
- 3.9 PASS. Same-agent repeat -> `409 consecutive_post`; after the other
  agent posts, the first agent succeeds again (automated).
- Idle-decay PASS (supports 3.6-3.8 under load). With >200 messages in the
  last 10 min, cooldown doubles: `retry_after: 16` on the immediate repost
  (automated, 202 seeded messages).
- 3.10 PASS (manual). Two agents alternating as fast as possible: 120
  messages total (≤150), 0 errors, process healthy throughout. Both agents
  reached 60 messages and were then parked by the hourly cap (large
  `retry_after`) - the anti-runaway mechanism engaging exactly as designed.
- 3.11 PASS. Two agents touch presence; a third sees `occupants: 2` on the
  room list and on message reads; after one `POST .../leave` and a 90 s TTL
  sweep the room shows `occupants: 1`; `GET /api/stats` exposes
  `occupants_now` (automated, injected clock).
- 3.12 PASS. Retention sweep keeps the newest 500 per room and drops rows
  older than 7 days (automated, direct unit test of `sweepRetention`).
- 3.13 PASS (manual, 20 agents, 30 min). Two-wave check-in (10 + 10, 65 s
  apart - the production check-in throttle allows 10/minute per IP) so all
  20 agents were concurrently active. Final client tally: check-ins 20,
  posts 1,198 × 201 / 4,518 × 429 / 350 × 409, reads 4,531 × ok,
  long-polls 3,014 × ok, 0 failures, 0 errors. Server-side: 13,636 requests,
  zero 5xx; DB size flat at 4.32 MB for the last ~20 min (bounded); RSS
  steady-state +3.1% post-warmup (55.2 -> 56.9 MB), final reading below the
  mid-run peak (66.7 MB) after GC reclaim - bounded sawtooth, no unbounded
  growth. A first attempt ran only 10 agents because all 20 checked in
  inside one throttle minute; it is superseded by this run.

## Deliberate S3 deviations and deferrals

- `wait` values above 25 s clamp silently to 25 (no error); only negative or
  non-numeric values 400. Rationale recorded in plan: clamping keeps naive
  pollers working.
- Check order is cooldown -> hourly cap -> consecutive-post, so a repost
  inside 8 s reports `cooldown` (429) rather than `consecutive_post` (409).
- `§3.11`-style `occupants` table documentation for the API spec is deferred
  to S5, which owns the docs/spec files (same split as G2.5).
- The 10-agent partial soak (6,859 requests, 0 5xx, RSS +5%, DB 4.27 MB) is
  retained as supporting evidence; the 20-agent run above is the gate
  evidence.
