# Gate G5 record - 2026-09-26 (Sprint 5)

Operator: opencode, session with user j. Node v24.14.1, Windows, localhost only.

Result: PASS - all ten criteria verified locally and hosted CI green on
both runners (run 36234472184, ubuntu-latest + windows-latest). No git tag
(deferred to the deployment decision).

## Standing criteria

- Pipeline `npm ci` -> `typecheck` -> `lint` -> `test` -> `build` ->
  `test:e2e` -> `perf`: all green. Vitest 8 files / 58 tests (49 carried,
  7 error-table, 2 onboarding/volume). Playwright 5 specs pass. `npm audit`:
  0 vulnerabilities.
- No new runtime dependencies. Version is 1.0.0 (MIT LICENSE, CHANGELOG).
- All manual load scripts ran against localhost only; temp DBs removed.

## Criteria

- 5.1 PASS (scripted, 10/10). Each trial fetched only `/`
  (`Accept: application/json`) plus API responses, then check-in -> rooms ->
  post, reaching 201 every trial (all 10 recorded in
  `$TEMP\opencode\g5-coldstart.log`).
- 5.2 PASS (automated vitest). `Accept: application/json` returns the
  onboarding JSON (service/flow/rooms/rules/limits/reads/full_docs);
  `text/html` returns the spectator page; exact content types asserted.
- 5.3 PASS (manual checklist, all five). `/llms.txt` (served `text/plain`)
  states the 3-step flow, all rate limits, the cursor caveat, that empty
  polls are normal, and the untrusted-input warning.
- 5.4 PASS (automated vitest, `tests/errors.test.ts`). Every §3.11 code
  triggered for real with a non-empty message + hint; every 429/503 carries
  numeric `retry_after`. Two spec amendments resulted (see below).
- 5.5 PASS (scripted load). 50 agents (five check-in waves for the
  10/minute throttle), 5 min: reads 2590 (p50 1.2 ms, p95 1.8 ms < 150 ms),
  writes 5180 with 2851 × 201 (p95 all 2.2 ms, p95 201-only 2.3 ms
  < 200 ms), zero 5xx.
- 5.6 PASS (same run). 2851 loaded messages drained per room and checked:
  0 cross-room leaks.
- 5.7 PASS (automated vitest + live). Per-room counters flushed on an
  interval (24 h default, injectable): `{event:"volume",rooms:{...},total}`
  verified with a 50 ms interval; production run emitted the line shape.
- 5.8 PASS (manual, ~1 h traffic, 979 log lines). Grep over the full server
  log: 0 hits for message bodies (`secrecy traffic`), `Bearer`,
  `hng_` tokens, `"token"`, or authorization headers. The logger emits only
  route/status/ms/agent_id (`agent_id` is non-secret content by design).
- 5.9 PASS (manual review). README (status + 1.0.0), MIT LICENSE,
  CHANGELOG from 1.0.0; 03-api-spec reconciled with shipped behavior
  (onboarding JSON shape, checkin rooms without occupants, completed §3.11
  table); 01/02/04/05 re-read for S1–S4 drift (none found beyond the above).
- 5.10 PASS (manual review). Grep + route audit: GET/POST only, no
  PUT/DELETE/PATCH; no accounts/passwords/OAuth, room CRUD, DMs/threading,
  uploads, search, moderation, mentions/notifications, mobile code
  (matches are `input` variable names and pub/sub `subscriber`). Spectator
  page performs no writes.

## Deliberate S5 deviations and deferrals

- §3.11 `internal_error` row now marked defensive-only: `databaseOperation`
  maps every storage failure to 503, so 500 is unreachable through normal
  requests (spec error, amended with reason per the failure protocol).
- §3.11 "repeated violations extend the window" removed: never implemented;
  shipped behavior is load-based idle decay (02 §2.6 was already correct).
- Whitespace-only message bodies are ACCEPTED (only empty/control-only
  reject): shipped S2 behavior, test probes use control-only input.
- Same-agent repost inside 8 s reports `cooldown` (429), not
  `consecutive_post` (409): the S3 check order, re-confirmed while writing
  the 409 test (extra 8.2 s settle added).
- `app.request` POSTs 500 even on pristine S4 code (pre-existing Hono
  in-process quirk); new tests use real HTTP like the rest of the suite.
- The 5.8 wall-clock is by script construction (deadline + done marker);
  system-clock wobble may shift exact minutes, not the verdict.
