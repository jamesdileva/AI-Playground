# Gate G0 record — 2026-09-17 (Sprint 0)

- Operator: opencode (Union Alpha), build-mode session with user j
- Result: PASS (all five criteria). Node v24.14.1, Windows (win32), localhost only.

## 0.1 - GET /api/health returns 200 with db_ok true - PASS

- Method: started compiled server (node dist/server.js), curl.exe to http://127.0.0.1:3000/api/health
- Raw output: HTTP/1.1 200 OK, cache-control: no-store, content-type: application/json; body: {"ok":true,"db_ok":true,"uptime_s":2,"version":"0.0.0-skeleton"}
- Server log lines (stdout): {"event":"listening","host":"127.0.0.1","port":3000} then {"event":"request","route":"/api/health","status":200,"ms":1,"agent_id":null}

## 0.2 - db_ok derived from a live query - PASS

- Automated: tests/http.test.ts closes the real better-sqlite3 connection in the fixture, then GET /api/health over real HTTP returns status 503 with ok:false, db_ok:false, error "unavailable", retry_after 5; healthy path returns 200 with db_ok true. (Re-verified by curl against the running compiled server before this record; see 0.1.)
- Note: the original chmod method was replaced in 05-verification-gates.md because file-permission changes do not fail an already-open SQLite connection on Windows; a closed connection fails the actual query deterministically. No public fault endpoint exists.

## 0.3 - Migrations idempotent, second run a no-op, still exactly 5 rooms - PASS

- Deleted hangout.db (and -wal/-shm); first npm run migrate created it: {"event":"migrated","path":"...hangout.db","version":2,"rooms":{"count":5}}; db-dump: rooms kitchen, balcony, couch, dancefloor, porch; counters total_checkins=0, total_messages=0; user_version=2; agents=0, messages=0
- Second npm run migrate: same output, version still 2, rooms still count 5, counters unchanged - a no-op. SELECT count(*) FROM rooms = 5 (recorded via scripts/db-dump.mjs, read-only connection)

## 0.4 - All five slugs present and correct - PASS (automated)

- Re-ran during this record: npm run migrate (no-op, version 2) and scripts/db-dump.mjs -> {"rooms":[{"slug":"kitchen"},{"slug":"balcony"},{"slug":"couch"},{"slug":"dancefloor"},{"slug":"porch"}],"agents":{"count":0},"messages":{"count":0},"counters":[{"key":"total_checkins","value":0},{"key":"total_messages","value":0}],"user_version":2}
- Automated test: tests/database.test.ts "seeds exactly the five contracted rooms and initializes counters" asserts exact slug order [kitchen, balcony, couch, dancefloor, porch] plus counter seeds, foreign_keys ON, clean foreign_key_check

## 0.5 - Pipeline succeeds end to end on Node 24 LTS - PASS

- Exact sequence npm ci -> npm run typecheck -> npm run lint -> npm test executed in order, twice locally; second run output: npm ci added 174 packages, 0 vulnerabilities; tsc clean; eslint clean after adding node globals for scripts/*.mjs; vitest 2 files / 6 tests passed; PIPELINE_PASS marker printed
- npm run build (tsc -p tsconfig.build.json) also ran clean and dist/server.js was started and health-checked for 0.1
- Hosted CI: workflow .github/workflows/ci.yml (Windows+Ubuntu, Node 24, read-only perms, no image build) will run on first push to origin; this record is the S0 local-allowance evidence per updated G0.5. Hosted run to be confirmed right after the first push.

## Deviations and notes

- 0.2 method amended from chmod to a closed-connection query failure (05-verification-gates.md updated); rationale recorded there
- 0.5 uses the documented S0 local allowance because origin was created only after implementation; hosted CI to be verified on first push
- All verification performed on localhost with loopback-only binding; no hosting, no Docker, consistent with the revised sprint plan
