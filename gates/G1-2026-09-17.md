# Gate G1 record - 2026-09-17 (Sprint 1)

Operator: opencode, session with user j. Node v24.14.1, Windows, localhost only.

Result: LOCAL PASS - all nine criteria verified. Hosted CI pending first push;
final G1 close-out requires that run green (same rule applied at G0).

## Standing criteria

- Pipeline `npm ci` → `typecheck` → `lint` → `test` → `build`: all green.
  Vitest 3 files / 16 tests. `npm audit`: 0 vulnerabilities.
- No new runtime dependencies: `hono/body-limit` and
  `@hono/node-server/conninfo` are subpath imports of existing packages.
- Health: `{"ok":true,"db_ok":true,...}` after all verification steps.

## Criteria

- 1.1 PASS. `POST /api/checkin` → 201 with agent_id (UUID), handle
  (`adjective-animal-NN`), one-time `hng_` token, visit_number, total_checkins,
  five rooms (slug/name/topic) and rules object. Automated
  (tests/checkin.test.ts G1.1) and manual curl.
- 1.2 PASS. 100 sequential check-ins; each response asserted
  `total_checkins == index + 1`. (G1.2)
- 1.3 PASS. Three batches of 50 parallel HTTP requests via `Promise.all`;
  per-run sorted counts equal `run*50+1 … run*50+50`, no gap or duplicate.
  (G1.3; injects `checkinLimit: 1000` so throttling is not the subject here;
  the production default 10/min is exercised by G1.8.)
- 1.4 PASS. Second check-in with same bearer token: same agent_id and handle,
  `visit_number: 2`, counter +1, token omitted, stored hash equals
  `SHA-256(token)`. Third visit reaches 3. Auth fixture route confirms
  middleware resolves agent from token. (G1.4)
- 1.5 PASS. Byte-scan of `hangout.db`, `hangout.db-wal`, `hangout.db-shm`:
  raw token present = false (Node `Buffer.includes`, post-shutdown).
  SQL: `token_hash` equals SHA-256 of issued token. (manual, this file)
- 1.6 PASS. `401 no_token` (missing header) and `401 bad_token` (invalid
  token) on a protected test fixture route; invalid bearer on `/api/checkin`
  also 401s. `/api/stats` is public by spec (test asserts 200 unauthenticated).
  (G1.6)
- 1.7 PASS. 500 check-ins → 500 unique handles, all satisfying
  `isHandleClean` (real blocklist + leetspeak folding). Separate test proves
  duplicate `preferred_handle` values disambiguate and blocked/invalid hints
  fall back safely. (G1.7 + companion tests)
- 1.8 PASS. With injected clock: 10 requests → 201; 11th →
  `429 checkin_throttle` with numeric `retry_after`; after advancing the fake
  clock 60 s the next request succeeds. (G1.8)
- 1.9 PASS. Raw token absent from captured server stdout/stderr during a full
  check-in → recheckin → stats sequence. Log lines carry route, status, ms,
  agent_id only. (manual, this file)

## Corrections made during verification (from earlier draft record)

- `/api/stats` was mistakenly gated behind auth; restored to public per §3.8.
  Auth-error checks now run against a protected fixture route instead.
- Added invalid-JSON (`400 invalid_json`), oversized-body
  (`413 body_too_large` via `bodyLimit` 4096 B) coverage; body parsing
  hardened (non-object bodies rejected).
- All door/database failures map to `503 unavailable` with `retry_after`
  instead of leaking raw SQLite errors; `Retry-After` header now sent.
- Returning-agent flow unified: one handler, auth optional, 201 either way.
- G1.7 blocklist assertion now uses the module's real `isHandleClean`.
- Log redaction tightened: only exact API routes logged; everything else
  logged as `/redacted`.

## Follow-ups

- Confirm hosted CI green after push (blocks final G1 sign-off per G0.5
  precedent).
- Post-push fix: G1.7 (500 sequential HTTP check-ins) exceeded Vitest's 5 s
  default timeout on the Windows runner (5014 ms) while passing on Ubuntu and
  locally. Test-only change: explicit 30 s timeout, no behavior change.
- `/llms.txt` and onboarding JSON (S5) still to come; check-in response is the
  only machine docs surface until then.
