# Gate G2 record - 2026-09-18 (Sprint 2)

Operator: opencode, session with user j. Node v24.14.1, Windows, localhost only.

Result: PASS - all twelve criteria verified locally and hosted CI green on both
runners (run 35411949293, ubuntu-latest + windows-latest).

## Standing criteria

- Pipeline `npm ci` -> `typecheck` -> `lint` -> `test` -> `build`: all green.
  Vitest 4 files / 27 tests. `npm audit`: 0 vulnerabilities.
- No new runtime dependencies (only Hono subpath imports already installed).
- Health `{"ok":true,"db_ok":true}` after verification; manual compiled-server
  run confirmed check-in, two isolated room conversations, counts, and 404s.

## Criteria

- 2.1 PASS. Write-then-read round-trips in all five rooms over real HTTP.
- 2.2 PASS. Isolation fuzz: 200 room-tagged nonces per run, 5 concurrent
  writers, 5 runs in one test (60 s timeout per the Windows CI lesson from
  G1.7). No sibling-room leakage; per-room counts exact every run.
- 2.3 PASS. Recursive scan of `GET /api/rooms` JSON finds no `body` key at
  any depth; counts and `last_activity_at` verified, null when empty.
- 2.4 PASS. `since=next_cursor` returns `[]` with unchanged cursor; replay
  from 0 reproduces history exactly once.
- 2.5 PARTIAL (behavior PASS, wording deferred). Naive `since+1` drain
  converges on the full ordered history despite non-contiguous global ids.
  The `/llms.txt` wording half belongs to S5, which owns that file; recorded
  here as a doc delta, not a failure.
- 2.6 PASS. `limit=2` pages 5 messages with `has_more` true/true/false;
  `limit=9999` clamps to 200 and returns all 5.
- 2.7 PASS. Empty and 1001-char bodies -> `400 body_invalid`; exactly 1000 ->
  201; control-only body -> 400; embedded controls stripped (`a<NUL>b<BEL>c`
  stored as `abc`). Rejected posts do not bump `total_messages`.
- 2.8 PASS. Unknown slug -> `404 no_such_room` naming all five slugs, on both
  GET and POST.
- 2.9 PASS. Unauthenticated POST -> `401 no_token`; unauthenticated GET ->
  200 with content.
- 2.10 PASS. Serializer unit test: a row with a mismatched `room_id` throws
  `Isolation violation` instead of leaking; matching rows serialize cleanly.
- 2.11 PASS (manual, once). Added a temporary `SELECT * FROM messages` probe
  outside `src/room/queries.ts`; ESLint failed with the Rule I message
  (`no-restricted-syntax`), then the probe was deleted. Same config runs in
  CI, so the check is enforced on every push.

## Manual compiled-server evidence (file-backed hangout.db)

- `POST /api/rooms/kitchen/messages` -> 201 `{"id":1,...,"next_cursor":1}`;
  balcony post -> id 2. Kitchen read returns only id 1, balcony read only
  id 2: isolation holds with global non-contiguous cursors as designed.
- `GET /api/rooms` shows per-room counts (kitchen 1, others 0) and no bodies.
- `GET /api/stats` -> `{"total_checkins":2,"total_messages":2,...}`.
- An early `invalid_json` during this session was a PowerShell/curl quoting
  artifact (inline `--data` mangling), not a server defect: identical bodies
  sent via `@file` succeeded. No code change resulted.

## Deliberate S2 deviations and deferrals

- `occupants` omitted from room list and message reads until presence lands
  in S3; a test pins the omission so S3 must consciously flip it.
- `wait` query param accepted but ignored (long-poll is S3); `wait=0`
  behavior is identical either way.
- New `400 bad_limit` code for invalid `limit` values (follows the S1
  precedent of `invalid_json`/`body_too_large`); `limit > 200` clamps.
- `POST` has no rate limiting yet; runaway protection is S3's scope and the
  risk is accepted until then.
